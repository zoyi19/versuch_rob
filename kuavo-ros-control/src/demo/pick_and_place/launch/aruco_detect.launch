<launch>

    <arg name="markerId"        default="582"/>
    <arg name="markerSize"      default="0.034"/>    <!-- in m -->
    <arg name="eye"             default="left"/>
    <arg name="marker_frame"    default="aruco_marker_frame"/>
    <arg name="ref_frame"       default=""/>  <!-- leave empty and the pose will be published wrt param parent_name -->
    <arg name="corner_refinement" default="LINES" /> <!-- NONE, HARRIS, LINES, SUBPIX -->
    <arg name="image_topic" default="/camera/color/image_raw" />         <!-- TODO: Your rgb image raw topic -->
    <arg name="info_topic" default="/camera/color/camera_info" />        <!-- TODO: Your rgb camera info topic -->
    <arg name="camera_frame" default="head_camera_color_optical_frame" />            <!-- TODO: Your rgb camera optical frame -->
    
    <node pkg="aruco_ros" type="single" name="$(anon aruco_single)">
        <remap from="/camera_info" to="$(arg info_topic)" />
        <remap from="/image" to="$(arg image_topic)" />
        <param name="image_is_rectified" value="True"/>
        <param name="marker_size"        value="$(arg markerSize)"/>
        <param name="marker_id"          value="$(arg markerId)"/>
        <param name="reference_frame"    value="$(arg ref_frame)"/>   <!-- frame in which the marker pose will be refered -->
        <param name="camera_frame"       value="$(arg camera_frame)"/>
        <param name="marker_frame"       value="$(arg marker_frame)" />
        <param name="corner_refinement"  value="$(arg corner_refinement)" />
    </node>

</launch>
